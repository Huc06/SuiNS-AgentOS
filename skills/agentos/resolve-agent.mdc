---
description: Resolve a SuiNS agent name via AgentOS registry — passport id, skills, dashboard URL.
globs:
alwaysApply: false
---

# Resolve Agent

```bash
agentos agent resolve alpha.sui --json
```

MCP: `agentos_resolve` with `{ "name": "alpha.sui" }`

Returns passport id, skills (MVR packages), and links to the manage-skills dashboard.
