{
  "name": "@agentos-sui/cli",
  "version": "0.0.1",
  "type": "module",
  "bin": {
    "agentos": "./dist/agentos.js"
  },
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "lint": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@agentos-sui/mcp": "workspace:*",
    "@agentos-sui/sdk": "workspace:*",
    "@mysten/sui": "^1.0.0",
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "tsup": "^8.0.0",
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
